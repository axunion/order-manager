{
  "name": "order-manager",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "devEngines": {
    "runtime": {
      "name": "node",
      "version": "^24",
      "onFail": "warn"
    },
    "packageManager": {
      "name": "pnpm",
      "version": "^11",
      "onFail": "warn"
    }
  },
  "scripts": {
    "prepare": "lefthook install || true",
    "dev:admin": "pnpm --filter @order/admin dev",
    "dev:order": "pnpm --filter @order/order dev",
    "dev:signup": "pnpm --filter @order/signup dev",
    "dev:api": "pnpm --filter @order/api dev",
    "build": "pnpm -r build",
    "check": "biome check && pnpm -r exec tsc --noEmit",
    "fix": "biome check --write",
    "test": "pnpm -r test",
    "db:generate": "pnpm --filter @order/db db:generate",
    "db:migrate": "pnpm --filter @order/api db:migrate",
    "db:reset": "pnpm --filter @order/api db:reset",
    "db:rebuild": "pnpm db:generate && pnpm db:reset",
    "db:studio": "pnpm --filter @order/db db:studio"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.0",
    "lefthook": "^2.1.9",
    "typescript": "^6.0.3"
  }
}
