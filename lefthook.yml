pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: "**/*.{js,ts,jsx,tsx,astro,json,jsonc}"
      run: pnpm biome check --write {staged_files}
      stage_fixed: true
    - name: typecheck
      run: pnpm astro check
    - name: test
      run: pnpm test
