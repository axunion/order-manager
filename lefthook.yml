pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: "**/*.{js,ts,jsx,tsx,json,jsonc,html}"
      run: pnpm biome check --write {staged_files}
      stage_fixed: true
    - name: test
      glob: "**/*.{ts,tsx}"
      run: pnpm test
