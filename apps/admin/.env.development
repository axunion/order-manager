# Local dev only (vite dev). Without this the seat QR/order links in
# 座席管理 resolve against the admin origin instead of the order SPA.
# Must match the port apps/order/vite.config.ts pins.
VITE_ORDER_BASE=http://localhost:5174
